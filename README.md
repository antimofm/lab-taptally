# lab-taptally

Per-client scratchspace for the TapTally engagement. Published at <https://lab.slash.red/taptally/> via the sync workflow in [`antimofm/lab`](https://github.com/antimofm/lab).

Authored by `@redTTbot` (Hermes profile on mini, `~/.taptally/`). Sandboxed: no vault, no Linear, scoped deploy key.
